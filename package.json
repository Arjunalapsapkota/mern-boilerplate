{
  "name": "mern-boilerplate",
  "jest": {
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/client/"
    ]
  },
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cd client && npm run-script test && cd .. && jest",
    "install": "cd client && npm install"
  },
  "engines": {
    "node": ">=8.0.0 <11.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "concurrently": "^5.1.0",
    "jest": "^25.1.0",
    "mocha": "^7.1.0"
  }
}
